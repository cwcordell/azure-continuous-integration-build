trigger:
  - master
  
pool:
  vmImage: 'ubuntu-latest'

jobs:
  - job: build
    steps:
    - script: echo Step 1
      displayName: 'Step 1'
    - script: echo Step 2
      displayName: 'Step 2'
    - script: echo Step 3
      displayName: 'Step 3'
    - script: echo Step 4
      displayName: 'Step 4'

